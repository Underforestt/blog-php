{% extends 'base.twig' %}

{% block body %}
    <div class="container">
        <img src="https://dummyimage.com/1200x400/2e48d9/ffffff" class="img-fluid" alt="...">
        <h1 class="mt-3 mb-3">{{ post.title|escape }}</h1>
        <div class="container mb-3">
            <div class="row">
                <span class="text-muted">Stas Glega | {{ post.published_date|date('F d, Y') }}</span>
            </div>
            <div>
                {{ post.content|raw }}
            </div>
        </div>
    </div>
{% endblock %}
